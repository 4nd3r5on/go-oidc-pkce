InternalServerError:
  description: Internal server error.
  content:
    application/json:
      schema:
        $ref: './schemas.yml#/ErrorResponse'
      examples:
        internalError:
          value: {"error": "Internal Server Error"}
